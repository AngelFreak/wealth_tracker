# Wealth Tracker Environment Configuration
# Copy this file to .env and customize the values

# ===========================================
# REQUIRED: Security Secrets
# ===========================================
# Generate these with: openssl rand -base64 32

# Session cookie signing key (MUST change in production)
SESSION_SECRET=change-me-generate-with-openssl-rand-base64-32

# Encryption key for broker credentials (MUST change in production)
# Must be exactly 32 characters
ENCRYPTION_SECRET=change-me-must-be-32-characters!

# ===========================================
# Server Configuration
# ===========================================

# Server host (use 0.0.0.0 for Docker)
HOST=0.0.0.0

# Server port
PORT=8080

# Environment (development or production)
ENV=production

# ===========================================
# Database
# ===========================================

# SQLite database path (relative to app directory)
# In Docker, this is inside the container at /app/data/
DB_PATH=/app/data/wealth.db

# ===========================================
# Optional: Timezone
# ===========================================

# Set container timezone (default: UTC)
TZ=Europe/Copenhagen
